# Change Log

## [0.0.7]

- Add optional `restart-language-server.debug` flag for detailed watcher/event logging.
- Default watch patterns now target `package.json`, `package-lock.json`, and `pnpm-lock.yaml` when `includes` is not set.

## [0.0.1]

- Initial release
